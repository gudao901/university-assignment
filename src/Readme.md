# CLIUniApp — University Application (CLI)

A command-line app for managing student registration, login, and subject enrolment, plus admin operations (show/group/partition/remove/clear). All data is stored in `students.data`.

## How to run

> macOS / Linux
```bash
python3 main.py
